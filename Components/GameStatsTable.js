import { Text, View } from 'react-native';


export default function GameStatsTable({navigation,}) {
  console.log(route.params)
    return (
      <View style={{ flex: 1, justifyContent: 'center' }}>
        <Text>table</Text>
      </View>
    );
  };